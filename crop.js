import { generateIF_1 } from './proxy';

const cropPerspective = generateIF_1('cropPerspective');
const cropRoundedCorner = generateIF_1('cropRoundedCorner');

export {
    cropPerspective,
    cropRoundedCorner,
}
